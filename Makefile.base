# $FreeBSD$

PACKAGE=lib${LIB}
LIB=    ifconfig

SHLIBDIR?= /lib
SHLIB_MAJOR= 1
SRCS=   libifconfig.c libifconfig_internal.c

INCSDIR=	${INCLUDEDIR}/libifconfig
INCS=   libifconfig.h

#MAN=    libifconfig.3

CFLAGS+=-I${.CURDIR}

.include <bsd.lib.mk>
